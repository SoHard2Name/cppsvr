#include "bits/stdc++.h"
#include "cppsvr/commfunctions.h"
using namespace std;

// 这个文件专门用于随便写点。。

int main() {
	// 测试数数 1e8 次是具体多久。
	uint64_t now=cppsvr::GetCurrentTimeMs();
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	for(int x=0;x<100000000;x++);
	cout <<(cppsvr::GetCurrentTimeMs()-now)/10.0<<endl;
	
	return 0;
}